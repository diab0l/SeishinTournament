<section class="upcoming-fights">
  <h2>Upcoming Fights</h2>
  <div>Current time: {{#now}}{{/now}}</div>
  {{#mats}}
  <div class="mat-upcoming-fights">
    <h3>{{name}}</h3>
    <table>
      <thead>
        <tr>
          <th>Time</th>
          <th colspan="3">Fighters</th>
          <th>Class</th>
        </tr>
      </thead>
      {{#fights}}
      <tr>
        <td class="time">{{startTime}}</td>
        <td class="name">{{fighter1.fullName}}</td>
        <td class="vs">VS</td>
        <td class="name2">{{fighter2.fullName}}</td>
        <td class="class">{{class.name}}</td>
      </tr>
      {{/fights}}
    </table>
  </div>
  {{/mats}}
</section>
